# Mock data mode - TEST HARNESS ONLY (`npm run dev:mock` / `vite --mode mock`).
# The Playwright smoke + screenshot suite renders the UI with no hub behind it;
# this is the only context that uses mockSource. Every other build/dev runs live.
VITE_DATA_MODE=mock
# Dev/test server mounts at the root.
VITE_BASE=/
