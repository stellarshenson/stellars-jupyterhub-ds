# Logs

Build and run logs for the stellars-jupyterhub-ds platform.

- `rebuild-portal.log` - output of `make rebuild` for the duoptimum-hub-web portal integration (node web-builder stage + wheel builds + image assembly)
