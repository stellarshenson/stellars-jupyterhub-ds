[pytest]
# The summary line ("N passed in Xs") is the total test execution time.
addopts = -ra
markers =
    spawn: best-effort lab-spawn smoke test (skipped if the minimal image does not spawn cleanly)
    gpu: GPU auto-detection test (skipped unless a GPU is detected on the host)
# pytest-playwright provides the page/browser fixtures (Chromium by default).
