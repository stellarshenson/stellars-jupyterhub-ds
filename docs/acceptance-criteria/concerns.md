# Acceptance Criteria - Consolidation Concerns

Potential cross-document conflicts found while consolidating the individual `acc-crit-*.md` docs into [acc-crit-duoptimumhub.md](acc-crit-duoptimumhub.md). Each line carries the feature/section, the specific criterion, the claim type (implemented-differently / not-relevant / duplicate / contradictory) and the explanation. Review pending - nothing here has been actioned.

- **Activity scoring** | criterion: "Real uncapped %" (Servers-page tooltip) | claim: contradictory | acc-crit-activity-consistency requires the activity meter shows the 7-day score consistently on every surface; acc-crit-activity-scoring adds an open criterion that the Servers tooltip shows an UNCAPPED % (which may exceed 100), while acc-crit-activity-consistency only checks for presence/value consistency. The two docs do not contradict the data path but the tooltip-value rule diverges: consistency says "identical meter", scoring says tooltip must be uncapped. Neither resolves which wins when the capped score (0-100 fill) and uncapped % differ in the tooltip text.

- **Activity scoring** | criterion: "Same tooltip on Servers, Users and the server resources widget" | claim: contradictory | this criterion is marked `[x]` (implemented), but the three open Servers-page tooltip criteria directly below it ("Real uncapped %", "Multiline", "Reflected in the design language") are `[ ]` (pending). The checked item claims the tooltip is already identical everywhere; the open items imply it is not yet in its final form. The two sets of criteria describe different states of the same surface.

- **Activity map freshness** | criterion: "Interval is env-configurable - JUPYTERHUB_ACTIVITYMON_STATS_INTERVAL (default 10s)" | claim: duplicate | acc-crit-cache-and-gpu describes the container stats cache interval as `JUPYTERHUB_ACTIVITYMON_STATS_INTERVAL` in the same module (`container_stats_cache.py`). acc-crit-activity-map-freshness covers the lazy snapshot mechanism; acc-crit-cache-and-gpu covers the GPU/volume cache family. The interval env var criterion is effectively stated twice across the two docs (one verifying the lazy stats cache, one verifying the broader cache-and-gpu infrastructure). Minor overlap only, not a conflict.

- **Background refresh** | criterion: "Boot prefetch - App.tsx::prefetchCore warms 12 list queries" (Prefetch section, appears TWICE in the doc) | claim: duplicate | the "Prefetch (already present)" section with its two criteria (`Boot prefetch` and `Edge: prefetch on nav hover`) appears verbatim twice in acc-crit-background-refresh.md (lines 28-33 and lines 49-54). One is a copy-paste duplication within the same document; the second occurrence should be removed.

- **Background refresh** | criterion: "Boot prefetch warms 12 list queries" | claim: contradictory | acc-crit-cache-and-gpu states "prefetchCore warms servers/users/groups/events/stats/resources/hub-info plus tokens/lab-container/settings/sent-notifications" (11 named queries, not 12). acc-crit-background-refresh says "12 list queries". The count differs by one; one of the two is stale.

- **Branding stage** | criterion: "Branding env namespace - JUPYTERHUB_BRANDING_FAVICON_BUSY_URI listed" | claim: not-relevant | FAVICON_BUSY_URI is listed in the namespace criterion but does not appear in the CLAUDE.md `Configuration Architecture` env-var table, and the functional test for the stage badge does not mention it. The acc-crit-functional-test-harness branding section plans a `JUPYTERHUB_BRANDING_FAVICON_BUSY_URI` test as `[ ]` (planned), confirming it exists but is not yet wired. Not a conflict, but the criterion bundles an unverified env var into an otherwise verified namespace list.

- **Broadcast autoclose** | criterion: "Five presets - 30s, 1min, 10min, 30min, 1h" | claim: contradictory | the criterion label says "Five presets" but the list contains six values: 30s, 1min, 10min, 30min, 1h - that is five values as written. However the CLAUDE.md description of broadcast says "140-character limit and live character counter" and "auto-close toggle (default: disabled)", implying the original design had an on/off toggle. The acc-crit replaces the toggle with five duration presets. acc-crit-functional-test-harness (Notifications broadcast section) still says "auto-close toggle" (line 491: "Page renders form - message field, type selector, auto-close toggle"). The harness criterion has not been updated to reflect the segmented-control change.

- **Cache and GPU** | criterion: "Env baked, not in compose - JUPYTERHUB_CACHED_DATA_TTL_MINUTES in Dockerfile + settings dictionary only" | claim: implemented-differently | the CLAUDE.md for the project lists `JUPYTERHUB_CACHED_DATA_TTL_MINUTES` is absent from `compose.yml`. The acc-crit states it is in the Dockerfile. A grep of compose.yml would confirm, but the CLAUDE.md env-var table does not list this key at all - it is plausible it was added after the CLAUDE.md was written, but the criterion cannot be verified from docs alone.

- **Cert provisioning** | criterion: "No legacy /mnt cert paths - no /mnt/certs or /mnt/user_certs in compose, Dockerfile, script, config, docs" | claim: not-relevant | the CLAUDE.md `TLS Certificates` section in the LOCAL DEPLOYMENT wrapper repo still references `./certs/tls.yml` and `./certs/_.lab.stellars-tech.eu/cert.pem` using `/certs`-style paths, which is consistent. However the wrapper repo is explicitly stated as out-of-scope for submodule edits (read-only). The criterion's scope ("both repos, clean") implies verification across both, but the submodule cannot be edited if the wrapper needs a cert-path update.

- **Advanced menu ordering** | criterion: "Alphabetical by label - Advanced children listed A->Z: Roles, Settings, Tokens" | claim: contradictory | acc-crit-roles-reference states the Roles page was added to the Advanced submenu. The alphabetical order R < S < T is correct for Roles, Settings, Tokens. However acc-crit-roles-reference does not mention the ordering rule, and if any future leaf is inserted it relies solely on the comment placed in nav.ts. The criterion is internally consistent but is only verifiable at runtime; the comment-based enforcement has no test coverage.

- **Drop the `/portal` URL Segment** | criterion: "Routes drop `/portal`" (and all other `[ ]` backend/frontend criteria) | claim: implemented-differently | The doc marks all backend and frontend criteria as `[ ]` (unimplemented), but `services/jupyterhub/duoptimum-hub-web/duoptimum_hub_web/__init__.py` and `handlers.py` show `ASSETS_ROUTE = r"/assets/(.*)"`, `BRAND_ROUTE = r"/brand/(.*)"`, `PORTAL_ROUTE = r"/(?!logo(?:/|$)|api/)(.*)"`, `PORTAL_URL = "/hub/dashboard"`, and `portal_handlers()` already wires all of these. The router basename and Vite base are also already at `/hub/` (config confirms `default_url = JUPYTERHUB_BASE_URL_PREFIX + PORTAL_URL`). Nearly every `[ ]` criterion in the Backend, Frontend, and Edge-cases sections is already implemented in code; the document's checklist state is stale.

- **Duoptimumhub Service + Image Rename** | criterion: "Hub bind/connect host - `c.JupyterHub.hub_ip` and `hub_connect_url` in config use `duoptimumhub`" | claim: implemented-differently | `config/jupyterhub_config.py:782` sets `c.JupyterHub.hub_ip = "0.0.0.0"` (all interfaces, not the hostname `duoptimumhub`) and `hub_connect_url` uses `socket.gethostname()` (line 778-779), not a hardcoded `duoptimumhub` string. Meanwhile `compose.yml:55` still has `hostname: jupyterhub` (not `duoptimumhub`). The criterion's description ("use duoptimumhub") does not match either the config code or the compose hostname; the crash-and-fix note in the log implies the real fix was `socket.gethostname()` + keeping `hostname: jupyterhub`, not a literal hardcoded `duoptimumhub` in the config.

- **Duoptimumhub Service + Image Rename** | criterion: "container_name - the literal suffix is `-duoptimumhub`" | claim: implemented-differently | `compose.yml:54` shows `container_name: ${COMPOSE_PROJECT_NAME:-stellars-jupyterhub-ds}-duoptimum-hub` (hyphenated `duoptimum-hub`, not the suffix `-duoptimumhub`). The criterion says the suffix is `-duoptimumhub` but the actual container name uses `-duoptimum-hub`.

- **Drop the `/portal` URL Segment** | criterion: "Landing-route rename" (marked `[ ]`) | claim: implemented-differently | The `PORTAL_URL = "/hub/dashboard"` and `PORTAL_ROUTE` are already set; the "Decision required" framing is stale. The implementation note inside the same doc says "Decision taken: home client-route renamed to `/dashboard`" - the criterion body contradicts its own `[ ]` status.

- **Portal Critic Sweep** | criterion: "[HIGH] Settings signup toggle is a dead control - uncontrolled defaultChecked wired only to a '(mock)' toast; no live persistence" | claim: contradictory | `acc-crit-functional-test-harness.md` has `[x] **Signup enabled/disabled** - signup form present iff JUPYTERHUB_SIGNUP_ENABLED=1` as implemented, implying the signup toggle is live. However the critic-sweep finding refers to the Settings *page* toggle (UI control in `Settings.tsx`), not the env-var gate. These are two distinct controls and the concern is legitimate - the functional test verifies the env-var effect, not the Settings page toggle persistence. Not a contradiction between docs per se, but the critic-sweep criterion risks being read as "signup doesn't work" when the env-var path does; recommend the label clarify "Settings page toggle" vs "env-var gate".

- **Compose Project Naming** | criterion: "Edge: stale wrapper compose" (marked `[ ]`) | claim: not-relevant | `docs/acceptance-criteria/.combine/concerns-2.md` note: `compose.yml` in the wrapper (`/home/konrad/@linux-workspace/local-stellars-tech-lab/compose.yml`) already passes `JUPYTERHUB_COMPOSE_PROJECT_NAME=${COMPOSE_PROJECT_NAME:-jupyterhub}` (line 127). The "stale wrapper compose" edge case is resolved - the wrapper is not stale. The `[ ]` criterion should be `[x]`.

- **Force Password Change** | criterion: "Dead 'Require password change' toggle - removed" (from acc-crit-critic-sweep `[x]`) vs "Toggle - 'Force password change on next login' switch on Configure-user" (from acc-crit-force-password-change `[x]`) | claim: not-relevant | The critic-sweep removal of the toggle in NewUser/BulkUsers is correctly scoped and does not conflict with force-password-change's toggle in UserConfig (Configure-user). No real concern - documented for clarity that these are different UI locations.

- **Functional Test Harness** | criterion: "Functional harness renamed - conftest.py BASE_URL/HUB_HOST default to `duoptimumhub`" (from acc-crit-duoptimumhub-rename `[x]`) | claim: implemented-differently | `tests/functional/conftest.py:24-25` shows `BASE_URL = os.environ.get("BASE_URL", "http://duoptimum-hub:8000/jupyterhub")` and `HUB_HOST = urlparse(BASE_URL).hostname or "duoptimum-hub"`. The default hostname is `duoptimum-hub` (hyphenated), not `duoptimumhub` (no hyphen). The duoptimumhub-rename criterion says the default is `duoptimumhub`; the actual default is `duoptimum-hub`. Minor naming inconsistency but could cause DNS lookup failures in the harness if the compose service name is `duoptimumhub` vs `duoptimum-hub`.

- **Group File Downloads** | criterion: "Resolved value - `resolve_group_config` returns `downloads_allow` as `True`/`False` when some group configures it, else `None`; the hook applies the platform default when `None`" | claim: implemented-differently | `resolve_group_config` was deleted per acc-crit-group-policy-model.md (v3.12.0); code at `policy/engine.py:32` exports `resolve_policies` as the drop-in replacement; the function name in this criterion is stale

- **Group Policy Model** | criterion: "Bundle (import/export foundation) = `{group_name, description, priority, policies}` where `policies` is the config dict" | claim: contradictory | acc-crit-group-policy-bundle.md (2026-06-17, post-dating this criterion) defines the bundle shape as `{groups:[{name, description, priority, policies:[{key, label, settings}]}]}` - hierarchical array of policy objects, not a flat config dict; the two docs describe incompatible wire formats for the same export action

- **Live Data Honesty** | criterion: "Import writes via real endpoints - `importGroups` creates each group then PUTs `/admin/groups/{name}/config`" | claim: contradictory | acc-crit-critic-sweep.md (same date 2026-06-17) lists "[MED] Import / Export groups are mockActions" as an OPEN item, contradicting the [x] fixed status here

- **Live Data Honesty** | criterion: "Export uses real configs - 'Export N groups' downloads `{groups:[{name, description, priority, config}]}` built from the live `/admin/groups` configs (raw flat `config` now carried on `GroupRow`)" | claim: contradictory | acc-crit-group-policy-bundle.md defines the canonical export bundle shape as `{groups:[{name, description, priority, policies:[{key, label, settings}]}]}` (hierarchical `policies` array, not flat `config`); the two docs describe incompatible wire formats for the same export action

- **Restart/stop progress feedback** | criterion: "Creeping bar / Active style / Rotating flavour line / Ready package / Untyped shim / Settle / Edge: error" (all `[x]`) | claim: contradictory | acc-crit-server-lifecycle-ux explicitly supersedes this entire doc: "No modal - the restart/stop progress modal (creeping bar + flavour text) is removed; `ServerLifecycle` is a context provider with no popup UI; `loading-messages` dep removed". Code confirms: `loading-messages` absent from `package.json` and not referenced in `src/`. Every criterion in acc-crit-restart-stop-progress is marked complete for a feature that no longer exists.

- **Navigation patterns** | criterion: "Start server - `/servers/:name/starting` returns to Home for your own server, Servers for another user's" (`[x]`, verified 2026-06-18) | claim: contradictory | acc-crit-server-lifecycle-ux §"Starting / restarting ANOTHER user's server - inline, no nav (#243, supersedes #237)" adds `[ ]` criteria stating admin starts must NOT navigate to `/servers/{user}/starting` at all. The navigation-patterns criterion assumes the admin DOES reach the start screen (returning to Servers from it); the lifecycle doc reverses that decision. Both cannot be correct simultaneously; the lifecycle doc explicitly notes "#243 supersedes #237".

- **Portal UI Polish** | criterion: "Desktop pill - gold 'Upgrade available' left of the status pill on the Server status card, running servers only" (`[x]`) | claim: contradictory | acc-crit-upgrade-available-pill states: "Label is 'Update available' - the user-facing pill label reads 'Update available' (capital U)" (renamed per operator 2026-06-18 in `ServerHero.tsx` + `MobileHome.tsx`). The portal-ui-polish doc retains the old label "Upgrade available" and was not updated when the rename happened.

- **Portal UI Polish** | criterion: "Animate the increase - on extend the bar should animate up to the new remaining, not snap" (`[ ]`) | claim: duplicate | acc-crit-ttl-extend-animation and acc-crit-ttl-progress-bar cover the extend animation in full (staged animation, 3s duration, optimistic boost, hold-until-refetch). The portal-ui-polish item is a stub that is fully superseded by those two dedicated docs; leaving it open here creates a false impression of an untracked gap.

- **Resource bars** | criterion: "GPU rows always show striped bars ... `GpuInventory` now unused" (`[x]`) | claim: implemented-differently | `services/jupyterhub/duoptimum-hub-web/src/components/meters.tsx:137` still exports `GpuInventory` as a named export. The criterion claims it is unused, but the symbol remains in the file. It is not imported elsewhere (confirmed by grep), so the claim is accurate as of the codebase state, but the dead export was not removed - a minor inconsistency worth noting for a future cleanup pass. No external consumer, so no functional impact.

- **Old portal cleanup** | criterion: "Shadowed auth/redirect templates - enhanced `login.html`, `signup.html`, `native-login.html`, `home.html`, `token.html`, `admin.html` are shadowed ... deleting them is auth-critical and wants an image rebuild to verify before removal" (`[ ]`) | claim: not-relevant | This is a deferred task, not a done criterion, which is fine. However, the parent doc (acc-crit-drop-portal-path) also calls out `home.html` as conflicting with the SPA's `/home` -> `/dashboard` rename. The cleanup doc does not reference the drop-portal-path constraint; an operator removing these templates without consulting drop-portal-path could break the SPA landing route. The two docs should cross-reference.

- **Server Status Immediacy** | criterion: "Runtime: no 10s lag" | claim: duplicate | same observable (status flips within ~2-3s after start/stop) is also covered as a runtime criterion in acc-crit-background-refresh.md ("Runtime: status flips within ~2-3s of start"), which already explains the adaptive poll mechanism that delivers this; the immediacy doc adds the spawner-authoritative front-end logic as the complementary piece, so both exist for distinct reasons, but an integrator may conflate them

- **Servers List Layout** | criterion: "Column order - Status, then Last activity, then Activity tracker" | claim: contradictory | acc-crit-servers-resource-cells.md (marked [x]) says "a 'Last activity' column sits immediately after Status" - this is consistent - but the resource-cells doc treats Last activity as a resource cell (grouped with cpu/mem/system edge cases), while servers-list-layout categorises it as a layout/ordering concern; the two docs overlap on the same column without cross-referencing each other

- **Start-server Page** | criterion: "Admin starting another user's server - lands on the page; on ready returns to the parent screen (Servers)" | claim: contradictory | acc-crit-server-lifecycle-ux.md section "Starting / restarting ANOTHER user's server (#243, supersedes #237)" explicitly states "No start-screen navigation - starting another user's server does NOT route to /servers/{user}/starting" and "Self-start unchanged - a user starting their OWN server keeps the start page (this only changes starting someone ELSE's server)"; the start-server-page criterion routes admin-starts to the page while server-lifecycle-ux reverses that with #243 superseding #237 - the two docs are directly contradictory for the admin-starts-other case

- **TTL Progress Bar** | criterion: "Upgrade-available pill - a gold 'Upgrade available' pill shows left of the status pill" | claim: duplicate | a dedicated acc-crit-upgrade-available-pill.md covers the pill in full; the TTL progress bar doc includes two criteria for this feature (pill + image-id-unknown edge) that are faithfully reproduced in the dedicated doc; having them in both creates a maintenance split (the label rename from "Upgrade" to "Update" was applied in the dedicated doc but the TTL bar doc still reads "Upgrade available" in the Home server-controls section)

- **"Upgrade Available" Pill** | criterion: "Label is 'Update available'" | claim: contradictory | acc-crit-ttl-progress-bar.md line "Upgrade-available pill - a gold 'Upgrade available' pill shows" was not updated when the operator renamed the label; the upgrade-pill doc correctly states the rename to "Update available" (2026-06-18), but the TTL bar doc (Home server-controls section) still reads "Upgrade available" - the two docs disagree on the displayed label

- **"Upgrade Available" Pill** | criterion: "Recency check - docker image ls newest local image for the repo vs the running container's image created time" | claim: contradictory | this criterion is in acc-crit-portal-ui-polish.md (marked [x]), not in this doc, but it describes the detection as a "created time" comparison; acc-crit-upgrade-available-pill.md explicitly states "ID comparison, not Created - the running image's Created is NOT read (it is gone from the store after a rebuild+prune)" and documents that the Created-vs-Created approach "could never fire"; the portal-ui-polish criterion is superseded and factually wrong about the algorithm

- **Version Sync Across Subpackages** | criterion: "Image packages only - the three subpackages are exactly the wheels the hub image installs (Dockerfile lines 174-176); 'duoptimum hub', 'jupyter hub services', and 'the other one' = docker-proxy" | claim: not-relevant | the criterion text leaves one package unnamed ("the other one") and uses informal language that would not survive a rename; Dockerfile line numbers are fragile references; the criterion is functionally correct but the informal phrasing weakens it as a verifiable acceptance criterion

## From acc-crit-portal-fixes.md (merged 2026-06-18)

Conflicts found when merging the portal de-mock + fixes punch-list into the master. Most are `duplicate` - the punch-list items were implemented and re-documented in dedicated sections; the `contradictory` / `implemented-differently` / `not-relevant` ones (lifecycle modals especially) are the ones to action.

- **Server lifecycle popups / Start popup** | criterion: "clicking Start server opens a modal with a progress bar" | claim: contradictory | "Server lifecycle UX (inline spinners, no modal, real log)" removed all lifecycle modals - start navigates to the dedicated Start-server page, not a modal

- **Server lifecycle popups / Restart popup** | criterion: "Restart opens an indeterminate 'Restarting container…' popup" | claim: contradictory | "Server lifecycle UX" states "No modal - the restart/stop progress modal is removed; ServerLifecycle is a context provider with no popup UI"; restart uses an inline spinner

- **Server lifecycle popups / Stop popup** | criterion: "Stop opens a 'Stopping server…' popup; while busy, controls are disabled" | claim: implemented-differently | "Server lifecycle UX" uses an inline spinner on the control, not a popup modal; the busy-disabling is retained but the mechanism differs

- **Server lifecycle popups / Mock parity** | criterion: "in mock mode the start popup animates to done so the demo shows the flow" | claim: contradictory | "Dedicated Start-server Page" specifies mock parity as the start PAGE with a canned log sample, not an animating popup modal

- **Server lifecycle popups / Use functional tests** | criterion: "drive these flows with the existing functional harness, extending the specs" | claim: not-relevant | "Functional Test Harness" (log 2026-06-18) replaced the stock-HTML specs wholesale with antd-SPA specs; there are no old specs to extend

- **TTL gadget / Full = blue** | criterion: "at ample time the bar reads 100% in standard blue" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" has [x] Full -> pct 100, blue

- **TTL gadget / Drain + colour** | criterion: "fill shrinks and shifts blue -> orange -> red as it nears the cull" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" has [x] Colour bands (danger <=20 min, warning <=60, accent above) + the two-phase drain rule

- **TTL gadget / Gray remainder** | criterion: "the used-up portion shows as a gray track behind the fill" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" has [x] Gray leftover (standard gray trail)

- **TTL gadget / Extend works** | criterion: "Extend issues the real POST extend-session and the bar/clock refresh" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" has [x] Extend invalidates hero and [x] Runtime: extend grows the bar

- **TTL gadget / Extend = hours input** | criterion: "Extend lets the admin type hours, validated and capped at max_extension_hours" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" has [x] Extend = hours input (InputNumber popover, min 1, max = round(maxAddHours))

- **TTL gadget / Edge: at extension ceiling** | criterion: "Extend disabled / no-op when max_extension_hours reached" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" has [x] At ceiling disables Extend

- **TTL gadget / Edge: server stopped** | criterion: "TTL gadget hidden or inert when no running server" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" has [x] Hidden when stopped

- **Live QA round 3 / Start -> dedicated page** | criterion: "own-server start navigates to a start page, no modal; restart/stop keep the lightweight popup" | claim: duplicate | "Dedicated Start-server Page" covers this; the popup half of the criterion is itself contradicted by "Server lifecycle UX" (no popups)

- **Live QA round 3 / Progress + spawn-log tail** | criterion: "progress bar bound to the spawn SSE plus a rolling tail of the last ~10 progress messages" | claim: implemented-differently | "Dedicated Start-server Page" clarifies the SSE message field is spawn-progress text, NOT container logs; the log feed comes from a backend tail endpoint

- **Live QA round 3 / Start popup: no Close on success** | criterion: "success auto-resolves; Close shows only on error (interim)" | claim: not-relevant | "Dedicated Start-server Page" was implemented (log 2026-06-17), making this interim modal criterion obsolete

- **Live QA round 3 / TTL bar base-relative** | criterion: "the drain bar measures against the BASE timeout, not the extension ceiling" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" [x] Two-phase pct + "Portal UI Polish" [x] TTL bar base-relative

- **Live QA round 3 / TTL hidden when server stopped** | criterion: "no progress bar rendered when the server is offline" | claim: duplicate | "TTL Progress Bar Behaviour Matrix" [x] Hidden when stopped + "Portal UI Polish" [x] TTL hidden when server stopped

- **Live QA round 3 / Volume reset spinner** | criterion: "the Reset button shows a loading state while the delete is in flight" | claim: duplicate | "Portal UI Polish" has [x] Volume reset spinner

- **Live QA round 3 / Duoptimum Hub version** | criterion: "footer + sidebar badge show the build-stamped package version" | claim: duplicate | "Version Sync Across Subpackages" covers the sync and "Portal UI Polish" covers the footer/badge display

- **Live QA round 3 / JupyterHub version banner** | criterion: "footer reads live from /hub/api/info; hardcoded STACK_CHIPS chip fixed to derive major from live version" | claim: duplicate | "Portal UI Polish" + "Live Data Honesty" cover the live hub-version derivation

- **Live QA round 3 / Preload core lists** | criterion: "servers/users/groups/events/stats prefetched at portal start" | claim: duplicate | "Last-known cache + non-blocking GPU" has [x] Key-page data warmed at start (prefetchCore)

- **Performance + persistent cache / Persistent query cache** | criterion: "the TanStack Query cache is saved to localStorage and rehydrated before first render" | claim: duplicate | "Background refresh + immediate update" / "Startup Hydration" cover persistCache/localStorage hydration

- **Performance + persistent cache / Background revalidate** | criterion: "hydrated queries are stale (past staleTime 30s), refetch on mount, keepPreviousData avoids blanks" | claim: duplicate | "Background refresh + immediate update" has [x] Adaptive poll on live queries + the keepPreviousData pattern

- **Performance + persistent cache / gcTime raised** | criterion: "in-memory cache kept warm 30 min (was 5)" | claim: duplicate | "Portal UI Polish" records the gcTime/keepPreviousData caching; the raise is already captured

- **Performance + persistent cache / Secrets excluded** | criterion: "token queries are never persisted to localStorage" | claim: duplicate | "Startup Hydration" confirms tokens are excluded from the prefetch warm list

- **Functional E2E harness / Port tests to the antd portal** | criterion: "rewrite the specs against the antd portal before it can gate cutover" | claim: not-relevant | "Functional Test Harness" (log 2026-06-18) states the harness was already rewritten to the live SPA, make test-functional-all (22 tests) green

- **Save user / group -> confirm + return to list** | criterion: "saving in Configure-user navigates to the Users list" | claim: implemented-differently | "Edit User Returns to Its Origin" + "Navigation patterns" navigate to backTo (origin: Home/Servers/Users), NOT always /users

- **Real per-GPU utilisation / Sampled via subcontainer** | criterion: "GpuUtilizationRefresher samples nvidia-smi in an ephemeral CUDA container on a periodic tick (30s)" | claim: duplicate | "GPU Utilisation Cache Logging" documents this exact mechanism (gpu_cache._refresh_sync, JUPYTERHUB_GPU_UTIL_UPDATE_INTERVAL 30s)

- **Real per-GPU utilisation / Snapshot field** | criterion: "/activity gpus[] gains utilization (int %) + memory_used_mb per device" | claim: duplicate | "Portal UI Polish" [x] Real utilisation + "gpuinfo-nvidia sidecar" cover these fields

- **BLOCKER: stale nginx mock portal / Single portal URL** | criterion: "after teardown, /portal 404s; the only portal is the hub-served live one" | claim: implemented-differently | "Drop the /portal URL Segment" specifies /hub/portal[/...] 302-redirects server-side (not a 404) per [x] Old-path redirect
